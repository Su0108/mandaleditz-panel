{
  "name": "mandaleditz-app",
  "version": "1.0.0",
  "description": "MANDALEDITZ native app (Capacitor wrapper around the existing web panel)",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@onesignal/capacitor-plugin": "^5.2.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
